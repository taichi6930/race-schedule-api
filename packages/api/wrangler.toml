name = "race-schedule-api"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[env.development]
# 必要に応じて環境変数やD1, KV, R2などのバインディングをここに追加

# ローカルでのポート指定（デフォルト: 8787）
# [dev]
# port = 8787

[env.test]
# ENVなどの値はGitHub Actionsのsecretで管理してください
# 例: ENV = "${ENV}"
